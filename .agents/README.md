# Agent Files

This directory contains agent-neutral workflow documents for this repository.

- `commands/` contains reusable operating procedures for release work,
  specification management, and live product testing.
- `AGENTS.md` remains the top-level repository guide for coding agents.

These files are intentionally system-agnostic. They should not assume a
specific assistant product, slash-command syntax, or vendor-specific metadata.
